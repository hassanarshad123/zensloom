[package]
name = "zensloom-camera-ffmpeg"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
ffmpeg = { workspace = true }
thiserror.workspace = true
tracing.workspace = true
zensloom-camera = { path = "../camera" }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[target.'cfg(target_os = "macos")'.dependencies]
cidre = { workspace = true }
zensloom-camera-avfoundation = { path = "../camera-avfoundation" }

[target.'cfg(windows)'.dependencies]
windows = { workspace = true }
windows-core = { workspace = true }
zensloom-camera-windows = { path = "../camera-windows" }

[dev-dependencies]
inquire = "0.7.5"

[lints]
workspace = true
