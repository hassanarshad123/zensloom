{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"inputs": ["**/*.ts", "**/*.tsx", "!src-tauri/**", "!node_modules/**"],
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"lint": {
			"cache": false
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"test": {
			"inputs": ["**/*.ts", "**/*.tsx", "!node_modules/**"],
			"cache": false
		}
	}
}
